# --- Python (desktop-legacy + server) ---
.venv/
**/__pycache__/
*.pyc
*.spec
build/
dist/
data/
*.db
*.db-*

# --- Node / Next.js (web) ---
node_modules/
.next/
out/
.vercel/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Secrets / env ---
.env
.env.*
!.env.example

# --- OS / editor ---
.DS_Store
Thumbs.db

# --- Source font library (full trial set lives at repo root; the app ships
# only the subset copied into web/app/fonts/) ---
/fonts/

# --- Claude Code runtime (transient) ---
.claude/scheduled_tasks.lock
.claude/*.lock
